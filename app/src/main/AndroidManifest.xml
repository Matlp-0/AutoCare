<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Busca do plano de manutenção do fabricante na internet (opcional: o app funciona offline). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".AutoCareApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".ui.onboarding.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.VehicleFormActivity"
            android:exported="false" />

        <activity
            android:name=".ui.onboarding.VehicleIdentificationActivity"
            android:exported="false" />

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.maintenance.AddMaintenanceActivity"
            android:exported="false" />

        <activity
            android:name=".ui.finance.FinanceActivity"
            android:exported="false" />

        <activity
            android:name=".ui.fuel.AddRefuelActivity"
            android:exported="false" />

        <activity
            android:name=".ui.importinvoice.ImportInvoiceActivity"
            android:exported="false" />

        <activity
            android:name=".ui.importinvoice.ConfirmExtractionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.history.MaintenanceDetailActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
